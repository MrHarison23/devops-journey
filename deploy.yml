script: |
  echo "${{ secrets.SSH_KEY_BASE64 }}" | base64 -d > key.pem
  chmod 600 key.pem

  ssh -i key.pem -o StrictHostKeyChecking=no david@ТВОЙ_IP << 'EOF'
    cd /home/david/devops-project
    git reset --hard
    git pull origin main
    docker compose up -d --build
  EOF
